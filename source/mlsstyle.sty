% This file is for Modelica specification figures what preamble.tex is for the main document (MLS.tex).
% All preamble content that figures and the main document have in common should go into mlsshared.sty.

\usepackage[fleqn]{amsmath} % Warning preamble.tex doesn't specify 'fleqn'.

% Warning: Don't set these lengths to zero in preamble.tex.
\setlength{\mathindent}{0pt}

% Define font size used for listings.
% This needs to go before mlsshared.sty is loaded.
\let\smallifpdf\small

\usepackage{ifpdf}
\usepackage{mlsshared}

% Settings for listings.sty in addition to those in mlsshared.sty.
% Compare additional settings for MLS.tex made in preamble.tex.
\lstset{ %
  xleftmargin=0pt,
  xrightmargin=0pt,
  aboveskip=0pt,
}

% Define colors only used in figures.
\definecolor{olive}{rgb}{0.20, 0.42, 0.15}
